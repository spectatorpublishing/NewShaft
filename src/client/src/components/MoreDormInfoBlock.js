import styled from "styled-components";
import React, { Component } from "react";

const DormInfoBlock = styled.div`
    display: flex;
`;

export default MoreDormInfoBlock = (props) => {

    return (
        <DormInfoBlock></DormInfoBlock>
    )
};

